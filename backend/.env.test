# Backend Test Environment Variables
# Used when running E2E tests

# Test Database (uses port 5433 to avoid conflict with dev database on 5432)
DATABASE_URL=postgresql://test_user:test_password@localhost:5433/pearly_test

# Secret key for JWT tokens (same as frontend's TEST_SECRET_KEY)
SECRET_KEY=test-secret-key-change-in-production

# Optional: Add other settings as needed for testing
# VIPPS_* keys can be test/mock values if needed

# This file is kept for backwards compatibility with imports
# All product models have been removed as product data is stored in Sanity CMS
# Only Pattern model remains in the database

# Note: Product schemas in app/schemas/product.py are still used for API validation
